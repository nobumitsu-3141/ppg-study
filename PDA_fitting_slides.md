# カーネル推定の手順スライド（5.3 の後半に追加）

`PPG_7.4_PDA_fitting.pptx` は、講義デック 7.4 の
`5.3 どうやって推測するのか`（51/68）の直後に 3 枚を追加したもの。

- 挿入位置：順番 84 枚目の直後（新しい順番 85〜87、ページ 52〜54）
- 総ページを 68 → 71 に更新し、挿入位置以降のページ番号を +3 で振り直し
- 書式は既存デックを踏襲（タイトル46pt・本文22pt以上・Meiryo・章ナビ・出典行、
  カード枠は F2F2F2 塗り＋2.25pt 色枠、注意帯は C00000）

既存の 83（当てはめの手順）と 84（どうやって推測するのか）が①〜④の粒度で流れを
示しているのに対し、追加分はその③「当てはめる」の中身を開くもの。

---

## 推定の流れ

前処理・切り出し → **モデル決定**（基底関数の種類と本数）→ **初期値と境界**
→ 最適化 → **検算** → 指標算出

このうちモデル決定・初期値・境界は、データではなく人間が決める設計値である。

## p.52 「5.3 初期値の決め方」

初期値の作り方は3方式あり、戦略は文献ごとに違う。

| 方式 | 内容 | 性質 |
|---|---|---|
| ランドマーク方式 | 微分でピークや変曲点を探し、その時刻を使う | 主流。目印が弱いと不安定 |
| 前の拍を引き継ぐ | 直前の拍で得た解を次の拍の初期値にする | 目印に依存しない |
| 多点スタート | 多数の初期値から当てはめて最良を選ぶ | 初期値依存を回避 |

注意帯：**初期値と同じくらい効くのが境界条件** ― `a > 0` ／ `μ₁ < μ₂ < …`（順序を固定）
／ `σ` の上下限。順序を固定しないと拍ごとに「第2成分波」が別物を指す。

基底関数が決めるのは、ランドマークをパラメータに直す翻訳の式だけ。Gaussian は μ が頂点の
時刻だがRayleigh は μ が立ち上がりの時刻（頂点は μ+σ）なので、μ = 頂点時刻 − σ で入れる。

## p.53 「5.3 当てはめの選び方」

「何を最小にするか（目的関数）」と「どう最小値を探すか（探索法）」は独立した選択。

| 目的関数 | 探索法 |
|---|---|
| **残差平方和 RSS ← 標準** | **Levenberg–Marquardt 法 ← 標準** |
| 重み付き最小二乗 WLS | 大域最適化（粒子群 ほか） |
| ロバスト損失 | 変数分離 VARPRO |
| ベイズ・正則化 | 逐次剥ぎ取り |

まとめ帯：目的関数はほぼ全文献が残差平方和。論文ごとに違うのは重み付けと探索
アルゴリズム。残差平方和は、ノイズが独立・正規なら最尤推定と一致する。

## p.54 「5.3 収束の検算」

| | 確認事項 | 意味 |
|---|---|---|
| ① | 境界に張り付いていないか | 張り付き ＝ モデルが窮屈な証拠 |
| ② | 振幅ゼロのカーネルが無いか | 実質使われていない ＝ 本数が多すぎる |
| ③ | 初期値を変えても同じ解か | 違う解に落ちるなら その拍では定まっていない |
| ④ | 残差プロファイルの幅 | 時間差 T を固定して当てはめ直し 谷の幅を見る |

注意帯：**残差が小さいことは検算の代わりにならない。**

③④のノートに入れた数値は合成波形による検証例（文献値ではない）。

- カーネル2本ではランダム初期値の 96% が同じ解に収束したが、6本では 1.3% のみ
- 切痕がある波形では T の区別できない範囲が ±5 ms 未満、切痕が消えた波形では
  75〜110 ms（幅 35 ms）。このとき最良解の残差はどちらも 0.9% 前後で差がない

---

## 検証

- `validate.py --original`：PASSED
- 川副式 `slide_lint.py`：総違反件数が元デック（101枚）と同一の 370 件。
  追加した3枚の新規違反は 0 件
- 実寸の目視確認は本番 PowerPoint（メイリオ）で行うこと。作業環境に LibreOffice の
  Impress フィルタがなく描画確認ができないため、テキストボックスはメイリオ想定の
  安全係数（幅 ×1.12・行高 ×1.18）で余裕を持たせてある

## 補足

このデック 7.4 は、別途 7.2 に追加した歴史スライド
（`PDA_history_slides.md` / `PPG_7.2_PDA_history.pptx`）を含んでいない別系統である。
両方を反映する場合は Mac 側の組版で統合が必要。
