# カーネル推定の手順スライド（5.2・5.3 に追加 / ver. 7.6）

`PPG_7.6.pptx` は、講義デック 7.4 → 7.5 → 7.6 と積み上げたもの。7.5 までは
`5.3 どうやって推測するのか`（51/68）の直後に 5 枚を追加したもの。

- 挿入位置：順番 84 枚目の直後（新しい順番 85〜89、ページ 52〜55）
- 総ページを 68 → 72 に更新し、挿入位置以降のページ番号を +4 で振り直し
- 図解スライドはページ番号を本体と共有（54/72 が 2 枚）
- 書式は既存デックを踏襲（タイトル46pt・本文22pt以上・Meiryo・章ナビ・出典行、
  カード枠は F2F2F2 塗り＋2.25pt 色枠、注意帯は C00000）

既存の 83（当てはめの手順）と 84（どうやって推測するのか）が①〜④の粒度で流れを
示しているのに対し、追加分はその③「当てはめる」の中身を開くもの。

---

## 推定の流れ

前処理・切り出し → **モデル決定**（基底関数の種類と本数）→ **初期値と境界**
→ 最適化 → **検算** → 指標算出

このうちモデル決定・初期値・境界は、データではなく人間が決める設計値である。

## p.52 「5.3 当てはめの中身」（概要）

手順①〜④のうち③「当てはめる」を開いた4段。カード4枚を矢印でつなぐ。

**① モデルを決める**（基底関数の種類と本数）→ **② 初期値と境界を置く**
→ **③ 残差を最小にする** → **④ 出た値を検算する**

注意帯：①② はデータではなく人が決める設計値。同じ波形でも決め方しだいで答えが変わる。
だから ④ の検算が要る。

## p.53 「5.3 初期値の決め方」

初期値の作り方は3方式あり、戦略は文献ごとに違う。

| 方式 | 内容 | 性質 |
|---|---|---|
| ランドマーク方式 | 微分でピークや変曲点を探し、その時刻を使う | 主流。目印が弱いと不安定 |
| 前の拍を引き継ぐ | 直前の拍で得た解を次の拍の初期値にする | 目印に依存しない |
| 多点スタート | 多数の初期値から当てはめて最良を選ぶ | 初期値依存を回避 |

注意帯：**初期値と同じくらい効くのが境界条件** ― `a > 0` ／ `μ₁ < μ₂ < …`（順序を固定）
／ `σ` の上下限。順序を固定しないと拍ごとに「第2成分波」が別物を指す。

基底関数が決めるのは、ランドマークをパラメータに直す翻訳の式だけ。Gaussian は μ が頂点の
時刻だがRayleigh は μ が立ち上がりの時刻（頂点は μ+σ）なので、μ = 頂点時刻 − σ で入れる。

## p.54 「5.3 当てはめの選び方」

「何を最小にするか（目的関数）」と「どう最小値を探すか（探索法）」は独立した選択。

| 目的関数 | 探索法 |
|---|---|
| **残差平方和 RSS ← 標準** | **Levenberg–Marquardt 法 ← 標準** |
| 重み付き最小二乗 WLS | 大域最適化（粒子群 ほか） |
| ロバスト損失 | 変数分離 VARPRO |
| ベイズ・正則化 | 逐次剥ぎ取り |

まとめ帯：目的関数はほぼ全文献が残差平方和。論文ごとに違うのは重み付けと探索
アルゴリズム。残差平方和は、ノイズが独立・正規なら最尤推定と一致する。

## p.54 「5.3 当てはめの選び方」（図解）

横軸をパラメータ、縦軸を残差とした「残差の地形」。谷が2つある曲線に、
異なる初期値から下る2本の矢印を描き、片方は局所解、片方は大域解に落ちる。

- 目的関数 ＝ 地形の形を決める（青）
- 探索法 ＝ 地形の下り方を決める（ティール）
- キャプション：同じ地形でも 初期値が違えば 別の谷に落ちる

曲線は custGeom のフリーフォームで作画。python-pptx の `build_freeform` は
局所座標を整数に丸めるため、インチで頂点を渡すと 1 インチ刻みの階段になる。
EMU で頂点を渡し `scale=1.0` とすること。

## p.55 「5.3 収束の検算」

| | 確認事項 | 意味 |
|---|---|---|
| ① | 境界に張り付いていないか | 張り付き ＝ モデルが窮屈な証拠 |
| ② | 振幅ゼロのカーネルが無いか | 実質使われていない ＝ 本数が多すぎる |
| ③ | 初期値を変えても同じ解か | 違う解に落ちるなら その拍では定まっていない |
| ④ | 残差プロファイルの幅 | 時間差 T を固定して当てはめ直し 谷の幅を見る |

注意帯：**残差が小さいことは検算の代わりにならない。**

③④のノートに入れた数値は合成波形による検証例（文献値ではない）。

- カーネル2本ではランダム初期値の 96% が同じ解に収束したが、6本では 1.3% のみ
- 切痕がある波形では T の区別できない範囲が ±5 ms 未満、切痕が消えた波形では
  75〜110 ms（幅 35 ms）。このとき最良解の残差はどちらも 0.9% 前後で差がない

---

## 検証

- `validate.py --original`：PASSED
- 川副式 `slide_lint.py`：総違反件数が元デック（101枚）と同一の 370 件。
  追加した5枚の新規違反は 0 件
- 実寸の目視確認は本番 PowerPoint（メイリオ）で行うこと。作業環境に LibreOffice の
  Impress フィルタがなく描画確認ができないため、テキストボックスはメイリオ想定の
  安全係数（幅 ×1.12・行高 ×1.18）で余裕を持たせてある

## 補足

このデック 7.4 は、別途 7.2 に追加した歴史スライド
（`PDA_history_slides.md` / `PPG_7.2_PDA_history.pptx`）を含んでいない別系統である。
両方を反映する場合は Mac 側の組版で統合が必要。

---

# ver. 7.6 で追加した図解スライド 3 枚

いずれも既存スライドの直後に置き、**ページ番号は元スライドと共有**（総ページは 72 のまま）。
図はすべて PowerPoint のネイティブ図形（フリーフォーム＋テキストボックス）で描いており、
PowerPoint 上で直接編集できる。

## p.47 「5.2 基底関数とは」（図解）― a・μ・σ

ガウス曲線 1 本に 3 つの寸法線を入れた図。

- **a**（青）＝ 高さ。曲線にかからないよう左端に縦の寸法線を置き、頂点から破線で導く
- **μ**（ティール）＝ 位置。基線の下に 0 から頂点までの横寸法線
- **σ**（ゴールド）＝ 幅。頂点の 0.607 倍（e^−0.5 倍）の高さで μ から μ+σ までの横寸法線

右に 3 枚のカード（a 高さ／μ 位置／σ 幅）を添え、注意帯は
**「成分波 1 本は この 3 つの数字だけで決まる」**。

ノートに、半値全幅 ＝ 2.355σ の関係（初期値づくりで σ を見積もる式）と、
Rayleigh では μ が立ち上がりの時刻で頂点は μ+σ になる点を記載。

## p.53 「5.3 初期値の決め方」（図解）― a・b・c

確認用ページの図 7 をスライドに移したもの。カード 3 枚。

- **a ランドマーク方式**：上段に波形、下段に 2 次微分。検出した時刻を赤点で示し、
  両者を破線で結ぶ
- **b 前の拍を引き継ぐ**：2 拍ぶんの波形。前の拍の解（塗り）から
  この拍の初期値（白抜き）へ矢印
- **c 多点スタート**：波形上にばらまいた出発点（青）と採用された解（赤）

注意帯は **「いずれも 出発点を決めているだけ ― 答えそのものではない」**。

## p.54 「5.3 当てはめの選び方」（図解）― 標準の 2 つ

左右 2 枚のカードで、それぞれの標準を図解。

- **左：目的関数の標準＝残差平方和**　実測（実線）とモデル（破線）の間に縦線を引き、
  「縦の差を 二乗して 全時点で足す」「ノイズが正規なら 最尤推定と一致」
- **右：探索法の標準＝Levenberg–Marquardt 法**　反復 0（10.9%）→ 反復 2（6.8%）→
  収束（1.2%）の 3 コマで、前進波（青）と反射波（金）が動いていく様子

注意帯は **「残差が減らなくなったら終了 ― 減らないことは 正しいことではない」**。

ノートに、1 反復の中身（①感度を調べる ②一手を解く ③良くなったか確かめる）と
停止条件 4 つ（残差が減らない／パラメータが動かない／勾配ゼロ／反復上限）を記載。
反復上限で打ち切られた場合は収束していない値が返るが見た目では区別できない、という注意も。

## 検証（7.6）

- `validate.py --original`：PASSED
- 川副式 `slide_lint.py`：総違反件数が元デック 7.5（106枚）と同一の 375 件。
  追加した 3 枚の新規違反は 0 件

## 作図の注意（再掲）

python-pptx の `build_freeform` は局所座標を整数に丸めるため、頂点をインチで渡すと
1 インチ刻みの階段になる。**EMU で頂点を渡し `scale=1.0`** とすること。

---

# ver. 7.8 で追加した「切痕と RI のずれ」2 枚

`4.1 自動解析の壁`（40/72）の直後に挿入。**ページ番号は 4.1 と共有**（総 72 のまま）。
図はネイティブ図形で描いており、いずれも合成波形による作図例（文献値ではない）。

## p.40 「4.1 切痕は見えるか」

**同じ2成分**（前進波 高さ1.00・位置0.16秒・幅0.060／反射波 高さ0.42・幅0.085）で、
反射波の戻る時刻だけを変えた2例を左右に並べる。各例とも上段に合成波、下段に分離した2成分。

| | 反射波の位置 | 前進波との差 | 合成波の見え方 | 従来法の P2 |
|---|---|---|---|---|
| 左 | 0.36 秒 | 200 ms | 切痕（極小）＋拡張期ピーク | 拡張期ピーク |
| 右 | 0.28 秒 | 120 ms | 極小なし、下降脚に肩 | 変曲点 |

注意帯：**同じ2成分でも 反射波が早く戻ると 切痕は下降脚に埋もれる**

要点は「成分は変わっていない」こと。右でも反射波の高さは 0.42 のままで、
見えなくなったのは切痕であって反射波そのものではない。

## p.40 「4.1 RI がずれる」

右（埋もれた例）で従来 RI と分離 RI を比較。

| | 値 | 定義 |
|---|---|---|
| 従来 RI | **0.563** | 変曲点の高さ 0.658 ÷ P1 の高さ 1.168 |
| 分離 RI | **0.420** | 反射波 a₂ ÷ 前進波 a₁ |
| 差 | **+0.143（+34% 過大）** | |

図では変曲点での高さの内訳を積み上げで示す。
**0.658 ＝ 前進波の裾 0.249 ＋ 反射波 0.409** で、従来 P2 の高さの約 38% は反射波ではない。

注意帯：**変曲点の高さの 38% は 前進波の裾 ― 反射波ではない**

切痕が明瞭な例では従来 RI 0.413 対 分離 0.420（差 −2%）、
P2 の高さ 0.424 の内訳は反射波 0.419 ＋ 前進波の裾 0.005 で前進波の寄与はほぼない。
つまり**切痕が見えているうちは従来法で十分で、埋もれた途端に系統誤差が現れる**。
時刻も ΔT 90 ms 対 真値 120 ms で 30 ms 短く出る。

## 検証（7.8）

- `validate.py --original`：PASSED
- 川副式 `slide_lint.py`：総違反件数が元デック 7.7（97枚）と同一の 347 件。
  追加した 2 枚の新規違反は 0 件
