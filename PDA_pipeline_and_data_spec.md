# PDA 実装仕様とデータ収集設計 ― 何を集め、どう解き、どう管理するか

**`afterload_research_design_roadmap.md` の Phase 0–2 を実行するための技術仕様**

> 本稿は研究計画（`afterload_research_design_roadmap.md`）を実装に落とすための作業仕様である。
> ① 集めるべきパラメータの辞書、② skewed-Gaussian × 2 カーネルによる PDA の前処理・境界条件・
> 推定・出力、③ 結果の集計単位、④ データ管理、⑤ 解析環境（MATLAB と Python）の選択、を扱う。
> 数値の閾値はすべて**事前規定すべき候補値**であり、Phase 0 の予備データで確定させる。

---

## 0. 先に決めておくべき 5 つ

着手前にこの 5 つを文書に固定する。後から決めると、解析の自由度が結果を作る。

| # | 決めること | 本稿の推奨 | 節 |
|---|---|---|---|
| 1 | カーネル数と形状拘束 | **skewed-Gaussian、2 カーネル、形状拘束（shape-tied）あり**。3 カーネル版を副次で併走 | §2 |
| 2 | サンプリング周波数 | **250 Hz 以上、可能なら 500 Hz** | §3.1 |
| 3 | 拍の採否基準 | SQI ＋ SE(τ) ＋ 境界張り付き ＋ 残差 RMSE の 4 条件（数値は Phase 0 で確定） | §6 |
| 4 | ベースラインの定義 | 導入直前の安静 2 分、SQI 通過拍の中央値 | §8.2 |
| 5 | 解析単位 | 拍ではなく**エポック**（介入前後の窓）。患者を変量効果とする混合モデル | §8 |

---

## 1. 集めるべきパラメータ

### 1.1 これを取り忘れると研究が死ぬ 6 項目

順に、失うものを書く。

| # | 項目 | 取り忘れると |
|---|---|---|
| 1 | **身長** | `SI = 身長 / ΔT` が計算できない。**SI が存在しなくなる。**術前訪問で必ず控える |
| 2 | **MAP の同期記録** | SI の定義式に MAP が入る（0.042×MAP）ため、MAP を調整できないと ΔSI の解釈が不可能になる。SVR の分子でもある |
| 3 | **HR の同期記録** | RI は心拍数の交絡を受ける。補正できず、主要評価が解釈不能になる |
| 4 | **CVP** | `SVR = 80(MAP−CVP)/CO` が計算できない。CVP 欠測時は「SVR 様指標」と明記するしかなくなる |
| 5 | **介入の正確な時刻と用量** | エポックが切れない。「いつ何をどれだけ入れたか」が秒単位で要る。麻酔記録の分単位では足りない |
| 6 | **測定条件メタデータ** | プローブ装着圧・末梢温・体位・フィルタ設定。これらは指標値を変える（Pilt 2024、Liu 2021）。**後から遡って取得できない唯一のカテゴリ** |

### 1.2 データ辞書

用途欄の A/B/C/D は `afterload_research_design_roadmap.md` §4 の Study A–D に対応する。

#### A 群 ― 波形（連続記録）

| 変数 | 単位 | 周波数 | 必須 | 用途 | 備考 |
|---|---|---|---|---|---|
| `PLETH` SpO₂ プレチスモグラフ | a.u. | **≥250 Hz** | ◎ | A B C D | **本研究の入力そのもの。**AGC・フィルタ後の表示波形か生波形かを必ず記録 |
| `ART` 観血動脈圧波形 | mmHg | ≥100 Hz | ○ | A B D | 参照。PPG との時相対応、SV 算出 |
| `ECG` 誘導 II | mV | ≥250 Hz | ○ | D | PAT/PWTT の起点。esCCO 系の再現に必須 |
| `CVP` 波形 | mmHg | ≥100 Hz | △ | B | 数値のみでも可 |

#### B 群 ― 数値トレンド（1–2 秒ごと）

| 変数 | 単位 | 必須 | 用途 | 備考 |
|---|---|---|---|---|
| `MAP` `SBP` `DBP` | mmHg | ◎ | 全 | 観血が望ましい。NIBP なら測定時刻を別に記録 |
| `HR` | bpm | ◎ | 全 | RI の交絡補正に必須 |
| `SpO2` | % | ◎ | 全 | |
| `PI`（灌流指数） | % | ◎ | B C | Coutrot 2019 の比較対照。末梢血管収縮の指標 |
| `CVP` | mmHg | ○ | B | SVR 算出 |
| `EtCO2` `RR` `換気設定` | ― | ○ | 全 | 呼吸性変動の解釈、PPV/SVV の妥当性 |
| `BIS` または `MAC` | ― | ○ | B C | 麻酔深度。血管拡張の主因 |
| `体温`（中枢・末梢） | ℃ | ◎ | 全 | **末梢血管収縮の最大の交絡。**末梢−中枢較差が有用 |
| `esCCO` `PWTT`（取得可なら） | L/min, ms | ○ | D | 実機からのみ |

#### C 群 ― 参照基準

| 変数 | 単位 | 必須 | 用途 | 備考 |
|---|---|---|---|---|
| `CO`（熱希釈／経肺熱希釈） | L/min | **B・D で ◎** | B D | **PPG／PWTT 信号系から独立でなければならない。**esCCO・FloTrac 由来は不可（ロードマップ §2.5.1） |
| `SVR` | dyn·s·cm⁻⁵ | ◎ | B | `80(MAP−CVP)/CO`。算出に用いた CO の出所を必ず記録 |
| `SVRI` | dyn·s·cm⁻⁵·m² | ○ | B | BSA 補正版 |
| `SV` | mL | ◎ | B D | |
| `Ea`（動脈エラスタンス） | mmHg/mL | ○ | B C | `≈ 0.9×SBP/SV`。esCCO 誤差は SVR より Ea とよく相関する（r=0.692） |
| 測定時刻 | ― | ◎ | B D | 間欠測定なら**各測定の正確な時刻**。エポックとの対応に必須 |

#### D 群 ― 患者背景（静的、1 症例 1 レコード）

| 変数 | 必須 | 用途 | 備考 |
|---|---|---|---|
| **身長** | ◎ | 全 | **SI の分子。最重要。** |
| 体重・BMI・BSA | ◎ | 全 | 指数化 |
| 年齢・性別 | ◎ | 全 | SI の主決定因子（0.086/年） |
| ASA-PS | ◎ | C | 基本モデルの共変量 |
| 高血圧・糖尿病・脂質異常・CKD・喫煙 | ◎ | C | 血管の硬さの背景 |
| **常用薬**（とくに RAAS 阻害薬・β 遮断薬・Ca 拮抗薬・利尿薬） | ◎ | C | **RAAS 阻害薬は導入後低血圧の既知の危険因子。**当日朝の内服有無も |
| 術前安静時 BP（外来値・入室時値） | ◎ | C | INPRESS の個別化目標の基準にもなる |
| 手術術式・予定時間・体位 | ◎ | 全 | |
| 術前心エコー（EF、E/e′、AS/AR の有無） | ○ | B C | 心側の交絡 |

#### E 群 ― 介入イベント（時刻つきログ）

**すべて秒単位の時刻**で。麻酔記録の分単位では不十分。可能なら投与ポンプのログを直接取得する。

| イベント | 記録する内容 |
|---|---|
| 導入薬 | 薬剤名・総量・投与開始と終了時刻（プロポフォール TCI なら効果部位濃度の時系列） |
| 筋弛緩薬・オピオイド | 同上 |
| **昇圧薬ボーラス** | 薬剤・用量（µg）・**投与時刻（秒）**。フェニレフリン／エフェドリン／ノルアドレナリンを区別 |
| 昇圧薬持続 | 流量変更の時刻と前後の値 |
| 輸液ボーラス | 種類・量・開始終了時刻 |
| 出血・尿量 | 時刻つき |
| 麻酔深度変更 | 吸入濃度・TCI 設定の変更時刻 |
| 気管挿管・体位変換・気腹・遮断解除 | 時刻。**強い外乱として除外区間にする候補** |
| 手術操作（牽引・臓器圧迫） | 分かる範囲で |

#### F 群 ― 測定条件メタデータ（**後から取れない**）

| 項目 | なぜ要るか |
|---|---|
| プローブの装着部位（示指／中指、左右） | 部位で波形形態が違う |
| プローブの種類・型番・波長 | 透過型／反射型で成因が異なる |
| **装着圧・固定方法**（テープ、手台、体側固定） | 経壁圧 10–20 mmHg でのみ指標が妥当という報告（Pilt 2024）。術中は手を被覆・固定するため影響が大きい |
| 手の高さ（心臓基準からの相対高） | 静水圧が拡張圧を変える |
| **モニタの機種・ソフトウェア版・フィルタ設定** | フィルタ条件が特徴点の時相をずらす（Liu 2021）。**症例間で変えない。変えたら記録する** |
| AGC の有無・表示波形か生波形か | 振幅絶対値の解釈が変わる |
| 室温・体温管理（加温装置の有無と開始時刻） | 末梢トーヌスの最大の交絡 |
| 記録開始／終了時刻、時計同期の方法 | **複数機器の時刻同期はズレる。**同期手順を決めて記録する |

#### G 群 ― 導出指標（計算されるもの、§7 の出力スキーマ）

拍ごと：`A, ξ, ω, α, Γ, τ` とその標準誤差、`RI, ΔT, SI`、残差 RMSE、SQI、採否フラグ。
エポックごと：各指標の中央値と MAD、Δ と %Δ。

---

## 2. PDA モデルの選択

### 2.1 skewed-Gaussian × 2 カーネルという選択の評価 ―― 妥当である

この組み合わせは、独立した 2 つの先行研究の結論を掛け合わせたものとして正当化できる。

| 根拠 | 内容 |
|---|---|
| **Basso 2024**（PMID 39577084） | MIMIC-III の 8,000 拍で、**skewed-Gaussian モデルが参照の Gamma–Gaussian モデルより有意に高精度**。ランダム初期値に対する感度が低く一貫して頑健。かつ **Gamma モデルより生理学的解釈が有効**であることを設計目標に掲げている |
| **Fleischhauer 2020**（PMID 33021236） | **カーネル 2 個のアルゴリズムがノイズ・体動に対して最も頑健**（14.09% の改善）で、形態情報の保存は多カーネルと同等。かつ「初期値の重要性」を強調。アルゴリズムと模擬データを MIT ライセンスで公開（github.com/FHDO-LAMBDA/PulseDecompositionAnalysis） |

すなわち **「非対称性は skewed-Gaussian で取り、カーネル数は 2 に抑えて頑健性を取る」**という設計であり、術中という**ノイズと体動が多い環境**にはとくに適合する。Tigges 2017 が示したとおりモデル選択は決着していないが、この選択には引用できる根拠がある。**この判断は支持する。**

### 2.2 ただし副作用が 1 つ ―― 2 カーネルの反射成分は「混合」である

2 カーネルは「前進波 ＋ 反射波 1 つ」を意味する。しかし本リポジトリの `iliac_reflection_identifiability.md` は、**腎反射（τ ≈ 70–140 ms）と腸骨反射（τ ≈ 180–450 ms）を別成分として扱う**設計を採っている。

2 カーネルにすると、この 2 つが 1 つの成分に吸収される。したがって推定される τ は

```
τ̂ ≈ （腎反射と腸骨反射の、振幅で重みづけた重心）
```

になる。問題は、**この重みが血管の状態で動く**ことにある。硬くなると腸骨反射が早期化して収縮期へ寄り、相対的な重みも変わる。すると τ̂ は、単一反射の到達時刻としてではなく、**混合比の変化としても動く**。

これはランダムノイズではなく**系統誤差**であり、ΔT（したがって SI）に方向性のあるバイアスを乗せる。振幅比 Γ̂ も同様に混合の影響を受ける。

| | 2 カーネル | 3 カーネル（形状拘束） |
|---|---|---|
| 頑健性 | **高**（Fleischhauer 2020） | 低い（自由度が増える） |
| τ の解釈 | 混合の重心。**生理的な帰属が曖昧** | 腸骨反射として帰属を試みられる |
| 識別可能性 | 良い | 形状拘束をかければ CRLB 2.5–6.9 ms（自リポジトリの実測） |
| 退化波形（融合） | 影響を受けるが解けはする | 形状拘束＋アンカーで救済（gross-fail 7.2%→1.2%） |

**推奨：両方を走らせ、2 カーネルを主解析、3 カーネル形状拘束を副次解析とし、両者の τ・Γ の一致（Bland–Altman）を報告する。**

これには 3 つの利点がある。第一に、モデル選択が決着していないという先行研究の状況（Tigges 2017）に対して、**選択の影響そのものを結果として報告できる**。第二に、2 カーネルが混合であることの実害を定量できる。第三に、`iliac_reflection_identifiability.md` の資産（形状拘束・アンカー・CRLB）を捨てずに済む。

計算コストは 2 倍になるだけであり、拍あたり数ミリ秒のオーダーなので実務上の障害にならない。

### 2.3 形状拘束（shape-tying）は強く推奨する

反射カーネルの形状（ω, α）を前進波と共有し、振幅比 Γ と遅延 τ だけを自由にする。

**理由 1：識別可能性が本質的に改善する。** 自リポジトリの合成波検証で、融合した最難ケースの CRLB が **±28.9 ms → ±6.9 ms（4 倍改善）**（`iliac_reflection_identifiability.md` §3.1）。

**理由 2：ΔT と RI の定義が確定する。** 形状が同一なら、2 つのカーネルのピーク位置のオフセットが相殺されるため

```
ΔT = τ           （厳密。ピーク探索が不要）
RI = Γ           （厳密。両カーネルのピーク値が等しいため、振幅係数の比＝ピーク高の比）
```

が**パラメータから直読できる**。形状を自由にすると、skew-normal のモード（ピーク時刻）は位置パラメータ ξ と一致しないため、ピークを毎回数値探索する必要が生じ、その探索誤差が ΔT と RI に乗る。

**理由 3：パラメータが減る。**

| モデル | パラメータ | 内訳 |
|---|---|---|
| 自由 2 カーネル | 9 | (A, ξ, ω, α) × 2 ＋ ベースライン |
| **形状拘束 2 カーネル** | **7**（ベースライン除去済みなら 6） | A, ξ, ω, α, Γ, τ ＋ ベースライン |
| 形状拘束 3 カーネル | 9 | 上記 ＋ Γ₂, τ₂ |

### 2.4 モデル定義

skew-normal（歪正規）カーネル。`φ` は標準正規の確率密度、`Φ` は標準正規の累積分布。

```
k(t; ξ, ω, α) = (2/ω) · φ((t−ξ)/ω) · Φ(α(t−ξ)/ω)
```

- `ξ`：位置（location）
- `ω`：尺度（scale）。パルス幅に対応
- `α`：歪度（shape）。`α > 0` で右裾が長い。PPG の前進波は「速い立ち上がり・緩い減衰」なので `α > 0`

形状拘束 2 カーネルモデル：

```
y(t) = A · [ k(t; ξ, ω, α) + Γ · k(t; ξ+τ, ω, α) ] + c
```

- パラメータ `θ = [A, ξ, ω, α, Γ, τ, c]`
- 出力：`RI = Γ`、`ΔT = τ`、`SI = 身長 / τ`

3 カーネル版（副次）：

```
y(t) = A · [ k(t; ξ, ω, α) + Γ₁·k(t; ξ+τ₁, ω, α) + Γ₂·k(t; ξ+τ₂, ω, α) ] + c
制約：τ₁ < τ₂
```

---

## 3. 前処理

### 3.1 サンプリング周波数 ―― なぜ 250 Hz 以上か

`SI = 身長 / τ` を τ で微分すると

```
∂SI/∂τ = −身長 / τ²
```

身長 1.65 m、τ = 0.20 s のとき **0.041 m/s per ms**。Millasseau 2002 の加齢係数は 0.086 m/s／年なので、

> **τ の 1 ms は、SI にして約 0.5 歳分**

に相当する。したがって

| 周波数 | 標本間隔 | 量子化のみの SI 誤差 | 年齢換算 |
|---|---|---|---|
| 100 Hz | 10 ms | ±0.21 m/s | ±2.4 歳 |
| 250 Hz | 4 ms | ±0.08 m/s | ±1.0 歳 |
| 500 Hz | 2 ms | ±0.04 m/s | ±0.5 歳 |

モデル当てはめは標本間隔以下の分解能を出せる（これがランドマーク法に対する PDA の主な利点の一つ）ため、100 Hz でも致命的ではない。しかし自リポジトリの CRLB（形状拘束で 2.5–6.9 ms）と比べて量子化誤差が同オーダーになる 100 Hz は、**余裕がない**。VitalDB の PLETH は 500 Hz であり、実機でも 250 Hz 以上を確保したい。

> **Phase 0 の確認事項**：実機（Life Scope）から PLETH 波形が**何 Hz で**取れるか。これが 100 Hz 未満なら、SI の精度設計を根本から見直す必要がある。

### 3.2 手順（この順序で）

| # | 処理 | 内容 | 注意 |
|---|---|---|---|
| 1 | **時刻同期** | 全機器の時計を揃える。手順を文書化し、症例ごとにオフセットを記録 | 後から直せない。ズレたエポックは使えない |
| 2 | **飽和・欠損の検出** | AD 上限下限に張り付いた区間、プローブ外れ、体動を区間としてマーク | 補間しない。区間ごと落とす |
| 3 | **低域通過** | 4 次 Butterworth、遮断 15–20 Hz、**ゼロ位相**（`filtfilt`） | **位相を進めない**。通常の IIR は時相をずらす（Liu 2021） |
| 4 | **ベースライン除去** | 拍の foot 点を通る**3 次スプラインを引いて減算** | **高域通過フィルタを使わない。**0.5 Hz のハイパスは拡張期の裾を歪め、反射成分そのものを削る |
| 5 | **拍分割** | foot 検出（1 次微分の極大の直前の極小、または交差接線法）。pyPPG の実装を使うと再現性が上がる | foot の定義を固定する |
| 6 | **信号品質評価（SQI）** | ① テンプレート相関（当該症例の中央波形との相関 ≥0.90）② 歪度 SQI ③ 拍長が中央値の ±30% 以内 | **当てはめる前に落とす。**悪い拍を当てはめると、悪いパラメータが出るだけ |
| 7 | **振幅正規化** | 各拍を `(peak − foot)` で除して振幅 1 に | **AGC 対策。**RI は比なので保存される |
| 8 | **窓の設定** | foot から**次の次の foot まで（2 拍窓）**。当てはめは 1 拍目を対象とし、2 拍目は裾の回収に使う | wrap-around（柔+速で反射が次拍に食い込む）対策。自リポジトリ §3.4 で残差が −4 ms → 0 になることを確認済み |

### 3.3 やってはいけないこと

| ✕ | なぜ |
|---|---|
| 高域通過フィルタでベースラインを除去する | 拡張期の低周波成分＝反射波そのものを削る |
| **時間軸を正規化する**（拍長で割る） | `ΔT` が失われ、**SI が計算できなくなる**。振幅のみ正規化する |
| 症例ごとにフィルタ設定を変える | フィルタが時相をずらすため、症例間で系統誤差が変わる（Liu 2021） |
| 欠損を補間して当てはめる | 補間値に当てはめた見かけのパラメータが出る |
| 当てはめ後に SQI で落とす | 計算資源の無駄。かつ「良い当てはめが出た拍だけ残す」選択バイアスになりうる |

---

## 4. 境界条件と事前分布

### 4.1 箱型制約（振幅正規化・時刻は foot からの秒）

| パラメータ | 下限 | 上限 | 根拠 |
|---|---|---|---|
| `A` | 0.05 | 5.0 | 振幅正規化後のスケール。広めに取ってよい |
| `ξ`（前進波の位置） | 0.02 s | 0.30 s | 収縮期ピークは foot から 100–200 ms |
| `ω`（尺度） | 0.02 s | 0.15 s | パルス幅の生理的範囲 |
| `α`（歪度） | 0.0 | 8.0 | **前進波は右裾が長い（α>0）。**8 を超えると形状が飽和する |
| `Γ`（反射係数） | 0.05 | 0.95 | 反射係数 `Γ=(Z_L−Z_c)/(Z_L+Z_c)` は 0–1。0 と 1 は除く |
| `τ`（遅延, 2 カーネル） | **0.10 s** | **0.45 s** | 腎（70–140 ms）と腸骨（180–450 ms）の**混合**を許す幅 |
| `τ₁`（3 カーネル・腎） | 0.07 s | 0.14 s | Baruch の窓 |
| `τ₂`（3 カーネル・腸骨） | 0.18 s | 0.45 s | 同上。`τ₁ < τ₂` を制約に加える |
| `c`（ベースライン） | −0.1 | 0.1 | ステップ 4 で除去済みなら固定 0 でよい |

### 4.2 τ への事前分布（アンカー）

自リポジトリの検証では、アンカーは**識別性（CRLB）をほとんど変えないが、誤収束を防ぐ**（gross-fail 7.2% → 1.2%、RMSE 27.5 → 12.2 ms）。**精度ではなく頑健性のための保険**として入れる。

```
τ̂_prior ≈ 2·c·身長 / PWV        σ_prior ≈ 30 ms
```

`PWV` は年齢からの推定値、または SDPPG の f 点から。目的関数に

```
残差ベクトルの末尾に  (τ − τ̂_prior)/σ_prior  を 1 要素追加
```

として組み込む（重み付き最小二乗の 1 観測として扱う）。

> **注意**：事前分布を強くしすぎる（σ_prior を小さくする）と、推定値が事前値に張り付き、**変化を追えなくなる**。本研究の主目的は Δ の追跡なので、σ_prior は緩く（30 ms 以上）取る。σ_prior の感度解析を必ず行う。

### 4.3 数値条件

- 時間軸は**秒**、振幅は**1 に正規化**して当てはめる。ms と生の AD 値のまま解くとヤコビアンのスケールが桁違いになり収束が悪化する
- `least_squares` の `x_scale='jac'` を使う（パラメータのスケール差を自動補正）

---

## 5. 推定

### 5.1 アルゴリズム

| 項目 | 選択 | 理由 |
|---|---|---|
| 最適化 | **信頼領域反射法（TRF）** `scipy.optimize.least_squares(method='trf')` | 箱型制約を扱える唯一の実用的な非線形最小二乗法 |
| 損失関数 | `loss='soft_l1'`（`f_scale` は残差スケールに合わせる） | 体動・スパイクへの頑健性。ただし**不確かさ推定は線形損失で取り直す**（§6.1） |
| 初期値 | **SDPPG ランドマーク（a–e, f 波）から生成** | 自リポジトリ §5 の方針。Fleischhauer 2020 も初期値の重要性を強調 |
| 多スタート | 3–5 点（ランドマーク由来 1 点 ＋ 境界内の準ランダム点） | Basso 2024 は skewed-Gaussian の初期値感度が低いと報告するが、**自データで確認する**（Phase 0 の検証項目） |
| 収束 | `ftol=1e-10, xtol=1e-10, max_nfev=2000` | |

### 5.2 最小実装

```python
import numpy as np
from scipy.stats import norm
from scipy.optimize import least_squares

def skew_kernel(t, xi, omega, alpha):
    """歪正規カーネル。形状拘束時はピーク正規化が不要（両カーネルのピーク値が等しいため
    振幅係数の比 Γ がそのままピーク高比 RI になる）。"""
    z = (t - xi) / omega
    return (2.0 / omega) * norm.pdf(z) * norm.cdf(alpha * z)

def model_2k(t, theta):
    """形状拘束 2 カーネル: theta = [A, xi, omega, alpha, gamma, tau, c]"""
    A, xi, omega, alpha, gamma, tau, c = theta
    return A * (skew_kernel(t, xi, omega, alpha)
                + gamma * skew_kernel(t, xi + tau, omega, alpha)) + c

def residuals(theta, t, y, tau_prior=None, sd_prior=0.030):
    r = model_2k(t, theta) - y
    if tau_prior is not None:                       # §4.2 のアンカー
        r = np.append(r, (theta[5] - tau_prior) / sd_prior)
    return r

# 箱型制約（§4.1）
LO = np.array([0.05, 0.02, 0.02, 0.0, 0.05, 0.10, -0.1])
HI = np.array([5.00, 0.30, 0.15, 8.0, 0.95, 0.45,  0.1])

def fit_beat(t, y, theta0, tau_prior=None):
    res = least_squares(
        residuals, np.clip(theta0, LO, HI), bounds=(LO, HI),
        args=(t, y, tau_prior), method='trf',
        loss='soft_l1', f_scale=0.02,      # 残差スケール。Phase 0 で校正する
        x_scale='jac', ftol=1e-10, xtol=1e-10, max_nfev=2000)
    return res
```

導出量は形状拘束のおかげで直読できる。

```python
A, xi, omega, alpha, gamma, tau, c = res.x
RI = gamma                 # 反射指数（＝ピーク高比、厳密）
dT = tau                   # 前進波–反射波の時間差（秒）
SI = height_m / tau        # スティフネス指数 [m/s]
```

---

## 6. 不確かさと採否

**これを実装するかどうかが、本研究が先行研究に対して持てる最大の差分**である（`PPG_wave_decomposition_prior_art.md` §9.2 の軸⑤：識別可能性を出力として報告する運用は標準になっていない）。

### 6.1 拍ごとの標準誤差

収束解でのヤコビアンから共分散を近似する。

```python
def param_se(res, n_data):
    p = res.x.size
    dof = max(n_data - p, 1)
    s2 = 2.0 * res.cost / dof          # 残差分散の近似
    JTJ = res.jac.T @ res.jac
    cov = s2 * np.linalg.pinv(JTJ)
    return np.sqrt(np.diag(cov)), np.linalg.cond(JTJ)

se, cond = param_se(res, len(t))
se_tau, se_gamma = se[5], se[4]
```

> **注意**：`loss='soft_l1'` を使うと `res.cost` は単純な残差平方和ではないため、上式は近似になる。**採否判定に使うのは近似で足りるが、論文に載せる不確かさは、SQI を通った拍について `loss='linear'` で当てはめ直して算出する。**

### 6.2 採否規則（事前規定。数値は Phase 0 で確定）

| # | 条件 | 候補閾値 | 意味 |
|---|---|---|---|
| 1 | SQI 通過（§3.2 ステップ 6） | 相関 ≥0.90 | 波形そのものが使えるか |
| 2 | `SE(τ)` | **≤15 ms** | 自リポジトリの CRLB は 2.5–6.9 ms。その 2–3 倍を上限に |
| 3 | `SE(Γ)` | ≤0.05 | Phase 0 で分布を見て決める |
| 4 | 境界張り付き | どのパラメータも境界から 1% 以上離れている | 張り付き＝モデルがデータに合っていない |
| 5 | 残差 RMSE | ≤ 拍振幅の 3% | 当てはまりの絶対基準 |
| 6 | `cond(JᵀJ)` | ≤1e8 | 非識別の検出 |

**低信頼拍は捨てるのではなく、フラグを立てて残す。**採否率そのものが Study A の主要評価項目である（年齢層別・スティフネス層別の推定成功率）。

### 6.3 拍間の平滑化

採否を通った拍の τ・Γ に、**状態空間平滑**（緩いランダムウォーク＋拍ごとの SE を観測ノイズとする Kalman 平滑）をかけてトレンドを安定化する（自リポジトリ §5 のステップ 5）。

> 平滑化の時定数は**事前に固定**する。強すぎる平滑は追跡したい急性変化を消す。フェニレフリンボーラスの効果は 1–2 分で立ち上がるので、**時定数は 15–30 秒**を上限の目安にする。

---

## 7. 出力スキーマ

### 7.1 拍レベルテーブル（1 拍 1 行）

| 列 | 型 | 説明 |
|---|---|---|
| `case_id` | str | 匿名化研究 ID |
| `beat_idx` | int | 症例内の通し番号 |
| `t_foot` | float | 記録開始からの foot 時刻 [s] |
| `rr` | float | 拍長 [s] |
| `model` | str | `tied2` / `tied3` / `free2` |
| `A, xi, omega, alpha, gamma, tau, c` | float | 推定パラメータ |
| `se_tau, se_gamma, se_alpha, ...` | float | 標準誤差 |
| `RI, dT, SI` | float | 導出指標 |
| `rmse, cond_jtj, n_restart, converged` | float/int/bool | 当てはめ診断 |
| `sqi_corr, sqi_skew` | float | 信号品質 |
| `accept` | bool | §6.2 の 6 条件をすべて満たすか |
| `reject_reason` | str | 落ちた条件（複数可） |
| `hr, map, sbp, dbp, pi, spo2, temp_periph` | float | 同時刻の数値（最近傍で結合） |

### 7.2 エポックレベルテーブル（1 エポック 1 行）

| 列 | 説明 |
|---|---|
| `case_id`, `epoch_id`, `epoch_type` | `baseline` / `pre` / `post` / `excluded` |
| `t_start`, `t_end` | エポックの時刻範囲 |
| `intervention`, `drug`, `dose`, `t_event` | 介入の内容と時刻 |
| `n_beats_total`, `n_beats_accepted`, `accept_rate` | **採否率はここでも出す** |
| `RI_med`, `RI_mad`, `dT_med`, `dT_mad`, `SI_med`, `SI_mad` | 中央値と MAD（外れ値に強い） |
| `map_med`, `hr_med`, `pi_med`, `temp_med` | 交絡の同時集計 |
| `co_ref`, `svr_ref`, `sv_ref`, `ea_ref`, `co_source` | 参照基準と**その出所** |

---

## 8. 結果をどう集めるか ―― エポック設計

**解析単位は拍ではなくエポックである。**拍を解析単位にすると見かけの n が膨らみ、個人内分散と個人間分散が混ざる（ロードマップ §3.3）。

### 8.1 エポックの切り方

| エポック | 定義 | 用途 |
|---|---|---|
| **ベースライン** | 前投薬後・導入前の安静 2 分。体位変換・会話・NIBP 加圧の直後を除く | すべての Δ の基準 |
| **導入エポック** | 導入薬投与開始から挿管後 10 分。1 分刻みの連続窓 | Study C（導入後低血圧） |
| **介入 pre** | 介入時刻 `t_event` の `[t−60 s, t−10 s]` | Study B |
| **介入 post** | ボーラスなら `[t+30 s, t+120 s]`、持続変更なら `[t+180 s, t+300 s]`（定常到達後） | Study B |
| **除外区間** | 気管挿管前後 60 s、体位変換、気腹開始、遮断解除、明らかな体動、電気メス使用中 | 事前規定で一律除外 |

> **重要**：post の窓は**薬物動態に合わせて事前に決める**。フェニレフリンの効果は 1–2 分でピークに達し 10–15 分で減衰する。窓を後から動かすと、良い結果が出る窓を選んでしまう。

### 8.2 Δ の作り方

```
Δ指標 = 指標(post) − 指標(pre)          （絶対変化）
%Δ指標 = Δ指標 / 指標(pre) × 100        （相対変化）
ΔSVR も同一の pre/post 窓で算出
```

間欠熱希釈の場合、pre/post 窓に測定が入るよう**介入の前後で必ず測る**運用にする。これは臨床手順の設計であって解析の問題ではない ―― プロトコルに書き込む。

### 8.3 主要解析

| 解析 | 方法 |
|---|---|
| **トレンド一致（主要）** | `(Δ指標, ΔSVR)` の 4 象限一致率。除外ゾーンは `|%ΔSVR| < 10%` を事前規定 |
| 極座標プロット | 角度バイアス <±5°、radial LOA <±30°（Critchley 2011） |
| 交絡調整 | 線形混合モデル：`Δ指標 ~ ΔSVR + ΔMAP + ΔHR + Δ体温 + (1|患者)` |
| **陰性対照** | 同一モデルを ΔSI について実行。**ΔSVR の係数が有意でないことを確認する** |
| 感度解析 | 2 カーネル vs 3 カーネル、σ_prior の変更、採否閾値の変更、平滑化の有無 |

---

## 9. データ管理

### 9.1 ディレクトリ構成

```
ppg-afterload-study/
├── protocol/              … プロトコル、データ辞書、事前登録の写し、倫理審査書類
├── code/                  … 解析コード（Git 管理。ppg-study リポジトリとは別でよい）
├── data/
│   ├── raw/               … 【不変】.vital 原本。書き込み禁止にする
│   │   └── manifest.csv   … ファイル名・SHA-256・取得日時・機器・症例 ID
│   ├── interim/           … 拍レベルテーブル（Parquet）
│   ├── processed/         … エポックレベルテーブル（Parquet）
│   └── external/          … VitalDB、Charlton 2019 仮想被験者
├── results/               … 図表、モデル出力
└── logs/                  … 解析実行ログ（日時・コードのコミットハッシュ・環境）
```

**`raw/` は一度書いたら二度と触らない。**加工は必ず新しい階層に出力する。原本を上書きすると復旧できない。

### 9.2 ファイル形式

| 用途 | 推奨 | 理由 |
|---|---|---|
| 生波形 | **`.vital` を原本保存**し、解析時に読み出す | Vital Recorder の圧縮形式。Python の `vitaldb` パッケージで直接読める。CSV に展開すると 100 倍以上に膨らむ |
| 拍・エポックテーブル | **Parquet** | 列指向・圧縮・型を保持。数百万行でも速い。CSV は型が壊れる（日時が文字列化する等） |
| 症例台帳（CRF） | **REDCap**（推奨）／なければ CSV ＋ データ辞書 | REDCap は監査証跡・入力規則・権限管理を持つ。多くの大学病院に既に導入されている |
| 図表 | PDF（ベクタ）＋ 生成コード | 後から再生成できることが重要 |
| コード・プロトコル | **Git** | 既に運用中 |

**データ本体は Git に入れない。**サイズと個人情報の両面で不適。データの版管理が要るなら DVC、または `manifest.csv` にハッシュを記録する運用で足りる。

### 9.3 匿名化と法令

| 項目 | 対応 |
|---|---|
| 研究 ID | 通し番号（`PPG-001`…）。カルテ番号・氏名・生年月日を含めない |
| 対応表 | **別媒体に分離**して施錠保管。解析用データと同じ場所に置かない |
| 日時 | 相対時刻（記録開始からの秒）を基本に。手術日は必要なら年月まで |
| 準拠 | 「人を対象とする生命科学・医学系研究に関する倫理指針」。**観察研究であっても施設の倫理審査は必須。**オプトアウトで足りるかは施設判断 |
| 波形データの扱い | 生体信号は再識別リスクが低いとされるが、**手術日時＋施設が揃うと特定可能**になりうる。公開時は日時を相対化する |

### 9.4 再現性

- 解析実行のたびに**コードのコミットハッシュ**をログに残す
- 環境を固定（`requirements.txt` または `conda env export`）
- **乱数シードを固定**（多スタートの初期値生成に使う）
- 「図 3 を作ったコードはどれか」が 1 年後に分かる状態を保つ

### 9.5 バックアップ

**3-2-1 則**：3 つのコピー、2 種類の媒体、1 つは物理的に別の場所。生波形は再取得できない。

---

## 10. MATLAB とは何か

### 10.1 何であるか

**MATLAB** は MathWorks 社の**商用の数値計算環境兼プログラミング言語**である。名は **MAT**rix **LAB**oratory に由来し、行列演算を基本データ型に据えている点が出発点だった。1984 年に商用化され、現在に至る。

### 10.2 なぜ論文でよく見るのか

工学系、とくに**生体信号処理・制御工学・画像処理**の分野で、長く事実上の標準だった。以下が揃っていたためである。

- 信号処理・統計・最適化の関数群が **Toolbox** として整備されている
- 対話的に配列を見ながら試行錯誤できる（当時としては画期的だった）
- 大学が包括ライセンスを持つため、研究室では実質無料に見える

PPG／脈波分解の分野も例外ではない。実際、Zaunseder らのグループは PPGI 用ツールボックスを MATLAB で公開しており、**§2.1 で引用した Fleischhauer 2020 の PDA コード**（`github.com/FHDO-LAMBDA/PulseDecompositionAnalysis`、MIT ライセンス）も同グループの公開物である。

> **確認事項**：同リポジトリの実装言語は本稿執筆時点で未確認である。同グループの他の公開物が MATLAB であることから MATLAB の可能性が高いが、参照する際に実際に確かめること。**もし MATLAB なら、それが本プロジェクトで MATLAB を使う唯一の現実的な理由になる**（先行実装との比較・再現）。

### 10.3 費用と代替

| | |
|---|---|
| 費用 | **有償。**基本パッケージに加え Toolbox が個別売り（Signal Processing Toolbox、Optimization Toolbox など）。個人向け Home ライセンスは比較的安価。大学・病院は包括契約を持つことが多い |
| 無料の互換環境 | **GNU Octave**。MATLAB の文法をほぼそのまま解釈する。既存の MATLAB スクリプトを動かすだけならこれで足りる場合が多い（Toolbox 依存があると動かない） |
| オンライン版 | MATLAB Online（ライセンスが要る） |

### 10.4 本プロジェクトでどちらを使うべきか ―― Python でよい

**新規に始めるなら Python を推奨する。**理由は 4 つある。

| # | 理由 |
|---|---|
| 1 | **無料**であり、離島・単独施設という制約に合う。ライセンス更新が研究の継続条件にならない |
| 2 | **pyPPG がある。**Goda 2024（PMID 38478997）が 19,000 時間・9,100 万拍超で検証した特徴点検出器と標準化 74 バイオマーカーの実装。**Study A の比較対照（ランドマーク法）がそのまま手に入る。**README に既に挙げている |
| 3 | **`vitaldb` パッケージ**で VitalDB と `.vital` ファイルを直接読める |
| 4 | **`scipy.optimize.least_squares` が本仕様に必要なものを全部持っている** ―― 箱型制約（`bounds`）、頑健損失（`loss='soft_l1'`）、ヤコビアン出力（`res.jac`、§6.1 の不確かさ推定に必須）、自動スケーリング（`x_scale='jac'`）。§5.2 のコードがそのまま動く |

必要な依存は `numpy` `scipy` `pandas` `pyarrow`（Parquet）`matplotlib` `vitaldb` `pyppg` 程度である。

**MATLAB が要る場面は限定的**で、①先行研究の公開コードを再現・比較したいとき、②共同研究者が MATLAB で書いているとき、の 2 つに尽きる。①は Octave で足りることが多い。

> **併せて**：本リポジトリの `iliac_reflection_identifiability.py` は既に `numpy`・`scipy` で書かれている。**環境の選択は事実上すでに済んでいる。**本仕様はその延長線上にある。

---

## 11. 参考文献

**モデル選択の根拠**

**[1]** Basso G, Haakma R, Vullings R. A skewed-Gaussian model for pulse decomposition analysis of photoplethysmography signals. *Physiol Meas*. 2024;45(11). PMID 39577084. <https://pubmed.ncbi.nlm.nih.gov/39577084/>
　（MIMIC-III の 8,000 拍。参照の Gamma–Gaussian モデルより有意に高精度。ランダム初期値への感度が低く一貫して頑健。Gamma モデルより生理学的解釈が有効であることを設計目標に置く）

**[2]** Fleischhauer V, Ruprecht N, Sorelli M, Bocchi L, Zaunseder S. Pulse decomposition analysis in photoplethysmography imaging. *Physiol Meas*. 2020;41(9):095009. PMID 33021236. <https://pubmed.ncbi.nlm.nih.gov/33021236/>
　（**カーネル 2 個が最も頑健**（14.09% 改善）で形態保存は多カーネルと同等。Gamma と Gaussian の組合せが優位。初期値の重要性を強調。アルゴリズムと模擬データを MIT ライセンスで公開：<https://github.com/FHDO-LAMBDA/PulseDecompositionAnalysis>）

**[3]** Tigges T, Pielmus A, Klum M, et al. Model selection for the Pulse Decomposition Analysis of fingertip photoplethysmograms. *Annu Int Conf IEEE Eng Med Biol Soc*. 2017:4014-7. PMID 29060777. <https://pubmed.ncbi.nlm.nih.gov/29060777/>
　（7,805 拍、AICc によるモデル選択。最良モデルでも全拍の 28.1% にとどまる＝**モデル選択は決着していない**）

**実装基盤**

**[4]** Goda MÁ, Charlton PH, Behar JA. pyPPG: a Python toolbox for comprehensive photoplethysmography signal analysis. *Physiol Meas*. 2024;45(4). PMID 38478997. <https://pubmed.ncbi.nlm.nih.gov/38478997/>

**[5]** Lee HC, Park Y, Yoon SB, et al. VitalDB, a high-fidelity multi-parameter vital signs database in surgical patients. *Sci Data*. 2022;9(1):279. PMID 35676300. <https://pubmed.ncbi.nlm.nih.gov/35676300/>

**測定条件の交絡**

**[6]** Liu H, Allen J, Khalid SG, Chen F. Filtering-induced time shifts in photoplethysmography pulse features measured at different body sites: the importance of filter definition and standardization. *Physiol Meas*. 2021;42(7). PMID 34111855. <https://pubmed.ncbi.nlm.nih.gov/34111855/>
　（**表題そのものが本稿 §3.3 の根拠。**フィルタ条件・測定部位・両者の交互作用がいずれも p<0.001 で特徴点の時相を変える。二次微分でのシフトが最大、部位では指が最大。**フィルタ定義の標準化と固定が必須**）

**[7]** Pilt K, Reiu A. Effect of transmural pressure on the estimation of arterial stiffness index from the photoplethysmographic waveform. *Med Biol Eng Comput*. 2024;62(4):1049-59. PMID 38123887. <https://pubmed.ncbi.nlm.nih.gov/38123887/>
　（経壁圧 10–20 mmHg でのみ PPGAI 対 AIx@75 が良好。**プローブ装着圧が指標値を変える**ため、装着法を固定し記録する必要がある）

**統計**

**[8]** Critchley LA, Yang XX, Lee A. Assessment of trending ability of cardiac output monitors by polar plot methodology. *J Cardiothorac Vasc Anesth*. 2011;25(3):536-46. PMID 21419654. <https://pubmed.ncbi.nlm.nih.gov/21419654/>

**本リポジトリ内**

- `afterload_research_design_roadmap.md` ― 本稿の上位計画。Study A–D とゲート基準
- `iliac_reflection_identifiability.md` ― 形状拘束・アンカー・CRLB の合成波検証。**本稿 §2.3・§4.2・§6 の直接の根拠**
- `PPG_wave_decomposition_prior_art.md` ― モデル選択の先行研究と、識別可能性報告という残された新規性
- `PPG_reflection_wave_localisation.md` ― 測定条件の交絡（§1.5）と反射波の帰属限界

---

本稿は研究実装のための技術仕様であり、臨床上の意思決定を指示・保証するものではない。閾値として挙げた数値は事前規定すべき候補値であって、確立された基準ではない。Phase 0 の予備データによって確定させること。
