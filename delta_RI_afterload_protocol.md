# ΔRI で術中の後負荷変化を見る ― 設計判断書

**「反射指数の個人内変化（ΔRI）から術中後負荷を追う」という研究方針に対して、定義・PI との弁別・
観測系・基準点・参照基準の五つの論点に決定を与える文書**

---

## 本稿の位置づけ

`reflection_wave_position_RI_afterload.md` は ΔRI を術中後負荷のトレンド指標にするという**作業仮説**を
提示した。`PPG_reflection_wave_localisation.md` §7–8 はその位置推定を landmark 法から**分解法**へ書き換え、
`PPG_wave_decomposition_prior_art.md` は分解由来 SI・RI が**既出**であり、残る新規性は指標定義ではなく
**検証条件の側**にしかないことを示した。

本稿はその続きで、**実際に研究を始めるために決めなければならないこと**だけを扱う。各論点について
「何が問題か」ではなく「どう決めるか」を書く。決定に根拠がある場合は根拠を、ない場合は「これは
恣意的な選択なので事前登録して固定する」と明記する。

第 2〜6 節の決定のうち計測系に関わるものは、同梱の `delta_RI_measurement_robustness.py`（合成波・真値既知）
で数値的に裏づけた。**この検証が答えているのは「計測系が ΔRI を壊すか」であって、「ΔRI が後負荷を映すか」
ではない。**後者は実患者と参照基準を要する（§6）。

本稿は提案であり、臨床判断の根拠としてはならない。

---

## 0. 五つの問いへの短答

| 問い | 決定 |
|---|---|
| **見てどうする** | 後負荷の**絶対推定はしない**。「変化の向きと大きさを一致率で当てる」に目的を固定する。振幅側の絶対値相関は先行研究で一貫して弱く（Couceiro 2015）、そこで勝負すると必ず負ける（§1） |
| **定義** | **二層定義。**一次＝分解由来 Γ₂（第 2 反射／前進波の振幅比）、参照＝landmark RI（T3 型限定の錨）。**変曲点への暗黙のフォールバックは禁止**し、「検出不可」を出力として残す（§2） |
| **PI との違い** | PI は**振幅の絶対量**、RI は**同一拍内の比**。AGC で PI は壊れ RI は壊れない（実験2）。ただし臨床では両者が同方向に動く場面が多く、**ΔPI で調整しても ΔRI が残るか**を示さないと独立した価値を主張できない（§3） |
| **観測** | **原波形が第一。**ただしモニタ相当の帯域制限下でも、**フィルタ条件を既知としてモデル側にも同じフィルタを通せば ΔΓ₂ は完全に回復する**（実験1、回復率 1.00）。したがって最優先作業は「フィルタ特性の同定」である（§4） |
| **基準** | **二重ベースライン。**B0＝入室後覚醒安静（個人特性・波形類型の確定用）、B1＝導入後・執刀前の安定期（術中トレンドの起点）。目的が違うので一つにまとめてはならない（§5） |
| **参照基準** | SVR を第一とするが、**単独では足りない。**介入（フェニレフリン）を参照に据え、**HR 交絡を設計で潰す**。HR だけで landmark RI は 10% 動く（実験3）（§6） |

---

## 1. 見てどうする ― 目的を勝てる形に固定する

### 1.1 目的の書き換え

「非侵襲的に後負荷を推定する指標を作りたい」は、そのままでは検証可能な形になっていない。**推定（絶対値）**と
**追随（変化）**は別の主張であり、要求される精度も統計も異なる。本プロジェクトの設計思想（個人内 Δ 追跡、
校正不要）と整合するのは後者だけである。

> **決定 1.1** ―― 目的は「後負荷の絶対値推定」ではなく、**「後負荷を動かす介入に対して、ΔRI が同方向に・
> 用量依存に動くことを示す」**に固定する。評価は相関係数ではなく**一致率（concordance rate）と polar plot**
> で行う。

これは逃げではなく、先行研究の失敗の構造から導かれる選択である。分解由来の振幅比を血圧・総末梢血管抵抗と
対比した Couceiro 2015 は、**すべての参照値と低相関**だった。振幅側の絶対値相関という土俵は既に負けが
確定している。一方で「個体内で介入をかけたときの変化の追随性」は先行研究が誰も評価しておらず
（`PPG_wave_decomposition_prior_art.md` §9.2 軸 1・軸 4）、統計的要求も相関より緩い。

### 1.2 陰性でも情報が出る形にする

> **決定 1.2** ―― 「ΔRI は術中の後負荷変化を追随しない」という結論が出た場合にも意味が残るよう、
> **同一の拍・同一の介入で ΔΓ₂（振幅軸）と Δτ₂（時間軸）と ΔDicpleth を同時に算出**し、三者を並べて報告する。

理由は二つある。第一に、本リポジトリの他の提案（esCCO 較正係数 K の動的補正）は「PPG 形態から後負荷情報が
取れる」ことを前提にしており、陰性結果はその前提を否定する重要な知見になる。第二に、先行研究の示唆は
**時間軸のほうが振幅軸より有望**という向きであり（同 §10 の設計上の含意）、振幅側だけを測って落ちると
「振幅が悪いのか、枠組みごと悪いのか」が区別できない。

> **正直な但し書き。**理論上、後負荷（末梢インピーダンス）に対応するのは反射係数 Γ＝**振幅側**である
> （`reflection_wave_position_RI_afterload.md` §3）。しかし実測での成績は時間側のほうが良い。**理論が指す軸と
> 実測が通る軸が食い違っている**というのが現状であり、本研究はこの食い違い自体を測ることになる。この点を
> 曖昧にしたまま「ΔRI で後負荷が見える」と書いてはならない。

---

## 2. 定義をどうするか

### 2.1 二層定義

現状の選択肢は「反射波ピーク」か「変曲点」かの二択で提示されていたが、**この二択自体が landmark 法の
枠内にある**。波形類型 T1b 以下では反射波ピークが局所最大として存在せず、変曲点は実装依存で決まる
（`PPG_reflection_wave_localisation.md` §6.3–6.4）。

> **決定 2.1** ―― 定義を二層に分ける。
>
> | 層 | 定義 | 適用範囲 | 役割 |
> |---|---|---|---|
> | **一次定義** | **Γ₂**＝分解の第 2 反射成分の振幅 ÷ 前進波振幅 | 全拍 | 主解析。局所最大を必要としない |
> | **参照定義** | **RI**＝拡張期ピーク高 ÷ 収縮期ピーク高（原波形） | T3 型のみ | 一次定義を検証する**錨**（§2.5） |
>
> 成分名は**順序**で呼ぶ（第 1 反射・第 2 反射）。腎・腸骨などの**部位名は用いない**——分解が返すのは順序で
> あって部位ではない（同 §7.8）。

### 2.2 変曲点をフォールバックにしない

pyPPG は拡張期ピークが局所最大として存在しないとき**一次微分の局所極大にフォールバック**し、f 点が
見つからないとき **f = e にフォールバック**する。いずれも警告なく発火する（同 §6.4）。

> **決定 2.2** ―― 「ピークが無ければ変曲点」という自動代替を**禁止**する。landmark 法は**検出可か検出不可
> かの二値**を返す実装に固定し、検出不可はそのまま記録する。フォールバックが発火した拍はログに残し、
> 層別解析する。

### 2.3 「ガウスフィッティングで必ず求める」は長所ではない

「ガウスフィッティングで反射波ピークを必ず求める」という案は、**必ず値が出ることを利点だと考えている点で
危険**である。最小二乗フィットは必ず一つの答えを返し、それがデータの決めた答えか初期値がたまたま落ちた
答えかは数字を見ても分からない。

実験4（合成波・真値既知・モデル不一致あり）が示した数値は次のとおり。真値は sech² 基底・3 反射・分散
β=1.5 で生成し、当てはめは非対称ガウス基底・2 反射・β=1.25 固定で行った（＝基底も成分数も分散も間違えた状態）。

| 波形類型 | landmark 検出率 | Γ₂ 誤差（相対） | τ₂ 誤差 | 初期値ばらつき |
|---|---|---|---|---|
| **T3**（τ₂=420 ms・明瞭） | 100% | 0.065（16%） | 7 ms | 0.0 ms |
| **T2**（τ₂=340 ms・浅い） | 0%（閾値依存） | 0.102（32%） | 5 ms | 0.0 ms |
| **T1**（τ₂=260 ms・融合） | 0% | 0.199（**83%**） | **230 ms** | 0.0 ms |
| **T0**（τ₂=180 ms・単峰） | 0% | 0.051（**43%**） | **191 ms** | 0.0 ms |

読むべき点は二つある。

1. **退化波形で分解の誤差は破滅的になる。**T1・T0 では τ₂ が 200 ms 前後ずれ、Γ₂ は真値の 4〜8 割ずれる。
   にもかかわらず**フィットは何事もなく値を返す**。
2. **初期値ばらつきはそれを検出しない。**ほぼ全条件で 0.0 ms、つまり「最適化はきれいに決まっている」。
   決まっているが**間違っている**。初期値ばらつき（およびプロファイル尤度幅・CRLB）が測っているのは
   **最適化が収束したか**であって、**モデルが正しいか**ではない。

> **決定 2.3** ―― 不確実性の出力は**必要条件であって十分条件ではない**。`PPG_reflection_wave_localisation.md`
> §8.2 の「事後分布幅で失敗を可視化する」だけでは不足であり、**モデル不一致を検出する別の手段**（§2.5 の
> 錨、および残差の系統性検定）を併置する。

### 2.4 基底関数・成分数・分散の扱い

> **決定 2.4** ―― 事前に固定し、事前登録する。
>
> - **基底**：非対称パルス（skewed-Gaussian を第一候補、sech² を感度分析）。左右対称ガウスは使わない。
>   Basso 2024 は MIMIC-III の 8,000 拍で skewed-Gaussian が従来モデルより高精度**かつランダム初期値に
>   対して頑健**と報告している。
> - **成分数**：前進波＋反射 2 成分に固定。拍ごとにモデル選択をしない（Tigges 2017 の「最良モデルが全拍の
>   28.1% でしか選ばれない」はモデル選択の不安定性の話であり、固定すれば消える）。
> - **形状拘束**：反射項は前進波の遅延・縮尺・分散コピーとする（shape-tying）。自由パルスにしない
>   ——`iliac_reflection_identifiability.md` §3.1 が示すとおり、これが識別性の本体である。
> - **分散 β**：まず固定。ただし実験4 は **β を間違えると τ₂ が 200 ms ずれうる**ことを示した。硬さで
>   dispersion が変わる可能性があるため、**β を緩く推定する条件を感度分析に必ず含める**。
> - **順序・生理拘束**：τ₁ < τ₂、Γ ∈ [0, 0.9]、前進波幅に上限（LVET 相当）。上限を置かないと前進波が
>   第 1 反射を飲み込む。

### 2.5 波形の正確性をどう評価するか ― 四層

「波形の正確性評価は？」への答えは単一の指標ではなく、**性質の違う四つを全部出す**である。

> **決定 2.5** ―― 1 拍につき次の四つを必ず出力し、推定値だけを下流に流さない。
>
> | 層 | 何を測るか | 具体 | 検出できるもの |
> |---|---|---|---|
> | **① 信号品質（SQI）** | そもそも解析に足る信号か | 体動・クリッピング・拍間の形状相関 | 体動アーチファクト、低灌流 |
> | **② 当てはめ残差** | モデルが波形を説明できたか | 残差 RMS／振幅比、残差の**自己相関**（系統性の有無） | モデル不一致（部分的に） |
> | **③ 識別性診断** | 最適化が一意に決まったか | 初期値ばらつき、プロファイル尤度幅、CRLB | 局所解・非識別 |
> | **④ 錨との一致** | 答えが正しいか | T3 型で分解 τ₂ ≟ 実測拡張期ピーク時刻、Γ₂ ≟ 実測 RI | **モデル不一致（本命）** |
>
> ③ は必須だが不十分（§2.3）。**④ が唯一、外から正しさを確かめる経路**である。T3・T2 の症例集団で
> ④ の一致が確認できて初めて、landmark が見えない類型へ外挿してよい。

さらに、③ に加えて**プロミネンス閾値の感度**を必ず報告する。実験4 の付随結果として、同一の T2 型波形で
prominence 閾値を 0.5%→1.0%→2.0% と動かすと検出率が **100%→80%→0%** と反転した。**「反射波が見えた／
見えない」は波形の性質ではなく実装パラメータの性質**でありうる。検出本数・検出率を一次アウトカムに
置いてはならない。

---

## 3. PI との違い

### 3.1 定義上は明快に別物である

| | **PI（perfusion index）** | **RI / Γ₂（reflection index）** |
|---|---|---|
| 定義 | AC ÷ DC（拍動成分 ÷ 非拍動成分） | 同一拍内の反射振幅 ÷ 前進波振幅 |
| 次元 | **振幅の絶対量**（DC で正規化） | **形（比）** |
| DC の影響 | 受ける（浮腫・組織厚・体温で DC が動けば PI が動く） | 受けない |
| AGC の影響 | **壊れる** | 壊れない |
| 主な決定因子 | 中枢（SV・CO・脈圧）と末梢（トーヌス・体温）の**積** | 反射係数 Γ（末梢インピーダンス）＋伝播 |

実験2 は AGC 耐性を数値で確認した。同一波形・同一ノイズにゲインのみ 0.5→4.0 倍（8 倍幅）を掛けたとき、

- AC 振幅（＝PI 相当）：0.513 → 4.101（**ゲインに正比例して変化**）
- landmark RI：0.4053 → 0.4053（**完全に不変**）
- 分解 Γ₂：0.3993、τ₂：419.9 ms（**完全に不変**）

これは本プロジェクトの設計原則 4.2（振幅の絶対量より比・時間を優先する）の数値的裏づけであり、
**PI に対する RI の構造的な優位はここにある**。臨床モニタが AGC をかけている限り、PI の絶対値・
拍間変化は信頼できないが RI は保存される。

### 3.2 しかし臨床では「別物」であることを示すのが難しい

問題は理論ではなく実測にある。**血管収縮が起きると PI は下がり RI は上がる**——両者は同じ生理から
反対向きに動くので、術中データでは強く相関しうる。相関する二つの指標を並べても、片方が独立した情報を
持つことにはならない。

したがって弁別は「定義が違う」ではなく、**解離する場面を見つける**か、**統計的に増分価値を示す**かの
どちらかで行う。

**解離が期待される場面（作業仮説）**

| 場面 | PI | RI／Γ₂ | 解離の理由 |
|---|---|---|---|
| **心拍出量低下のみ**（出血初期・前負荷低下） | ↓ | →（不変が期待） | AC は SV に依存、Γ は末梢インピーダンスに依存 |
| **末梢トーヌス上昇のみ**（フェニレフリン） | ↓ | ↑ | 両者が反対向き＝最も弁別しやすい |
| **低体温**（局所血管収縮・DC 変化） | ↓↓ | ↑（軽度） | PI は DC 側からも押し下げられる |
| **浮腫・長時間手術での組織変化** | ↓ | →（不変が期待） | DC のみの変化。RI は DC を含まない |
| **深麻酔・血管拡張** | ↑ | ↓ | 同方向の解離だが向きは逆 |

> **決定 3.1** ―― 「PI との違い」は主張ではなく**評価項目**として設計に組み込む。具体的には、
> **ΔSVR（または介入）に対する ΔΓ₂ の関連を、ΔPI で調整したうえでも有意に残るか**を事前規定の副次
> 評価項目とする（偏相関・多変量モデル）。ここが陰性なら、ΔRI は PI の言い換えにすぎないと結論する。

この設計は現実的でもある。PI は同じ SpO₂ センサから同時に取れるので、追加コストがゼロで交絡調整に使える。

> **補足（PI 側の実務知識）** ―― PI はプローブ装着指で系統的に変わる。示指と薬指の差が最小
> （0.07 ± 1.89%、p=0.74）で、他の組み合わせは有意差があるという報告がある（Shintani 2025, Cureus
> 17(4):e82131）。**プローブ交換は PI のベースラインを壊す**ので、§5 の再ベースライン規則に直結する。

---

## 4. どう観測するのか

### 4.1 対象波形 ― 原波形か、モニタ波形か

**結論から言えば「原波形で成立性を示し、モニタ波形で劣化を定量する」の二段構えだが、実験1 の結果は
モニタ波形について当初の想定より楽観的である。**

モニタ表示波形は増幅・高度フィルタ済みで、拍動成分を強調するよう最適化されている（Alian & Shelley 2014）。
フィルタは特徴点の時相をずらし、その影響は指で最大・二次微分で最大となる（Liu 2021）。そして**日本光電を
含む臨床モニタの AGC 実装と帯域特性はメーカー非公開**であり、本リポジトリでも区分 D（未確認）のままである
（`PPG_reflection_wave_localisation.md` §1.7）。

そこで実験1 では、遮断周波数を 0.05〜1.0 Hz（モニタ設定としてありうる範囲）で掃引し、**血管収縮相当の
介入（反射振幅 ×1.25）を与えたときに Δ がどれだけ回復するか**を測った。回復率 1.00 が「無フィルタと同じ
Δ が取れた」を意味する。

| 遮断周波数（因果的 1 次 HPF） | ΔRI（landmark）回復率 | ΔΓ₂（素朴な分解）回復率 | ΔΓ₂（filter-aware）回復率 |
|---|---|---|---|
| なし | 1.00 | 1.00 | 1.00 |
| 0.05 Hz | 1.00 | 1.00 | 1.01 |
| 0.10 Hz | 0.95 | 0.98 | 1.01 |
| 0.25 Hz | 0.87 | 0.93 | 1.00 |
| 0.50 Hz | 0.79 | 0.83 | **1.01** |
| 1.00 Hz | **検出失敗 100%** | 0.69 | **1.00** |

（ゼロ位相フィルタ＝事後処理の filtfilt では劣化が緩く、1.0 Hz でも landmark 0.86／素朴 0.91／filter-aware 1.00。）

読み取れることは三つある。

1. **帯域制限は Δ を系統的に過小評価させる。**0.5 Hz で 2 割、1.0 Hz で 3 割の Δ が失われる。絶対値だけで
   なく Δ も壊れるので、「Δ を追うから校正不要」という論法だけでは帯域制限を免れない。
2. **landmark 法は 1.0 Hz で完全に破綻する。**拡張期側の低周波成分が削られ、拡張期ピークが局所最大として
   検出できなくなる。
3. **フィルタ条件が既知なら、分解法は Δ を完全に回復する。**モデル側にも同じフィルタを通して当てはめる
   （filter-aware fitting）だけで、全遮断周波数で回復率 1.00 になる。**帯域制限は克服可能な障害であり、
   克服の条件は「フィルタ特性を知っていること」の一点に尽きる。**

> **決定 4.1** ―― 主解析は**原波形**（Vital Recorder / VitalDB、500 Hz）で行う。並行して、**モニタ側の
> フィルタ特性の同定を最優先の実務作業**とする。同定の経路は次の三つ。
>
> 1. 既知入力（矩形波・掃引正弦波）を光学的に与えて入出力から周波数応答を推定する
> 2. 同一患者・同時刻の原波形とモニタ出力波形を対にして伝達関数を推定する
> 3. メーカーへの照会・特許明細書からの復元
>
> 同定できた場合は filter-aware fitting を実装する。**同定できない場合、モニタ波形での本研究は成立しない**
> ——これは `esCCO_K_correction_hypothesis.md` が「帯域制限下で d/a・e/a が計算できるか」を必要条件として
> いるのと同じ構造の必要条件である。

### 4.2 何を用いて SI・RI を出すか

> **決定 4.2** ―― 二系統を**同一の拍に対して**走らせ、直接対決させる。
>
> | 系統 | 実装 | 出力 | 位置づけ |
> |---|---|---|---|
> | landmark 法 | **pyPPG**（Goda 2024。219 点ベンチマークで p1 の MAE 1 ms、f 点 3 ms） | RI, ΔT, SI, Dicpleth | 比較基準・§2.5 の錨 |
> | 分解法 | 自作（shape-tied、非対称基底、filter-aware） | Γ₁, τ₁, Γ₂, τ₂ ＋ 不確実性 | 主解析 |
>
> 切痕検出は Pal 2024 の IEM（iterative envelope mean）法を第一候補とする（PPG は動脈圧より低 SNR まで
> 頑健：検出可能 SNR は ABP ≥ −9 dB に対し PPG ≥ −12 dB）。
>
> SI＝身長 ÷ ΔT は**術前の個人特性としてのみ算出**し、術中トレンドには用いない。身長が定義に内在する
> ため個人内 Δ では消えるが、術中に身長は変わらないので SI の Δ は ΔT の Δ の言い換えにすぎない。

### 4.3 ノイズ・アーチファクトへの対応

> **決定 4.3** ―― 「1 拍ごとに正しい値を出す」ことを要求しない。血管の状態は秒〜分で動くので、
> **1 拍ごとの識別可能性を求めること自体が過剰要求**である（`PPG_reflection_wave_localisation.md` §7.7）。
>
> 1. **拍分割と SQI** で明らかな体動・クリッピング拍を落とす（除外率を必ず報告する）
> 2. **アンサンブル平均**：8〜16 拍を位相合わせして平均してから分解する。ランダムノイズは √N で落ちる。
>    ただし**平均する窓の中で血行動態が動いていない**ことが前提なので、窓長は事前規定し、昇圧薬ボーラス
>    直後のような急変時は窓を短くするか単拍解析に切り替える（切り替え規則も事前規定）
> 3. **状態空間平滑**：Γ₂・τ₂ に緩いランダムウォークを課し、拍ごとの識別性診断を観測ノイズとして重みづける
> 4. **棄却基準を事前に決める**。事後的に閾値を選ぶと選択バイアスが入る。**除外率は年齢層・波形類型別に
>    報告する**——ここを怠ると「解析できた拍だけ見たら関連があった」という最も陥りやすい誤りに落ちる

---

## 5. どこを基準と捉えるのか

### 5.1 二重ベースライン

「安静時か、導入後か」は**どちらか一方を選ぶ問題ではない**。二つは測っているものが違う。

- **導入それ自体が最大の介入である。**プロポフォール・吸入麻酔薬は交感神経を抑制して末梢血管を拡張させ、
  PI を上昇させる。ΔRI も当然大きく動く。
- 同時に、**波形類型（T3〜T0）と大血管スティフネスは覚醒時に決めておく必要がある**。これらは術中に
  変わらない個人特性であり、§2.5 の錨が使えるかどうか（＝その症例で landmark 検証が可能か）を決める。

> **決定 5.1** ―― ベースラインを二つ取る。
>
> | | **B0：入室後・覚醒安静** | **B1：導入後・執刀前の安定期** |
> |---|---|---|
> | 取得条件 | 仰臥位、プローブ装着後 5 分以上、会話なし | 気管挿管後 5 分以上、体位確定後、執刀前、昇圧薬なし |
> | 用途 | 波形類型の判定、SI・個人特性、**錨が使えるかの判定** | **術中 ΔRI トレンドの起点** |
> | 主解析での役割 | 層別変数 | Δ の基準点 |
>
> **主解析の ΔRI は B1 基準**とする。理由は、術中の臨床的関心が「導入後の安定状態と比べて今どうか」で
> あること、および導入という巨大な介入をまたぐ Δ には麻酔薬・体位・体温の変化がすべて混入することによる。
> **B0 → B1 の変化（導入による変化）は、独立した副次評価項目**として別に報告する（導入時低血圧の
> 予測という、それ自体が臨床的に意味のある問いになる）。

### 5.2 ベースラインが壊れる事象と再ベースライン規則

Δ 追跡は「ベースライン以降、測定条件が定数である」という仮定に依存する。この仮定が破れる事象を事前に
列挙し、規則を決めておく。

> **決定 5.2** ―― 次の事象が起きたら**再ベースラインを取り、Δ の連続性を切る**（連続したトレンドとして
> 扱わない）。
>
> | 事象 | 理由 | 対応 |
> |---|---|---|
> | **プローブの付け替え** | 装着圧（経壁圧）で指標値が変わる（Pilt 2024：経壁圧 10–20 mmHg でのみ良好）。指間差もある（Shintani 2025） | 再ベースライン必須。交換時は**示指↔薬指**を用いる |
> | **体位変換**（側臥位・砕石位・頭低位） | 静水圧と経路長が変わる | 再ベースライン |
> | **測定側上肢の神経ブロック・区域麻酔** | 局所トーヌスが全身と乖離する | 対側で測定するか、その症例を除外 |
> | **モニタのゲイン／フィルタ設定変更** | 時間指標が設定間で比較できない（Liu 2021） | 設定は術前に固定し、変更をログに記録 |
> | **体温の大幅変化**（>1.5°C） | 末梢トーヌスと DC が動く | 共変量として記録し、感度分析 |
>
> これらはすべて**記録項目**でもある。記録していなければ、後から交絡として扱うこともできない。

---

## 6. 何と関連させるのか

### 6.1 参照基準の階層 ― SVR は理想だが、単独では足りない

理論上の対応相手は末梢負荷インピーダンス Z_L ≒ SVR である。しかし SVR には実務上の問題が三つある。
**(i)** SVR＝(MAP − CVP)×80/CO 自体が導出量で、CO の測定誤差をそのまま引き継ぐ。**(ii)** 多くの手術症例で
CO も CVP も取れない。**(iii)** 指尖 PPG と SVR の相関はもともと中等度にとどまる（Middleton 2011, r=0.54）。

> **決定 6.1** ―― 症例を参照基準の強さで階層化し、**階層ごとに何を主張できるかを事前に決める**。
>
> | 階層 | 症例 | 得られる参照 | 主張できること |
> |---|---|---|---|
> | **A** | 心臓外科（肺動脈カテーテル・熱希釈 CO・CVP・観血圧） | **SVR、Ea（≒収縮末期圧/SV）** | 主解析。ΔΓ₂ ↔ ΔSVR の関連 |
> | **B** | 非校正 CO モニタ（FloTrac 等） | SVRI トレンド | 追随性の検証（絶対値は使わない） |
> | **C** | 観血動脈圧のみ | 拡張期圧・MAP・脈圧、昇圧薬投与量 | 介入前後の Δ（§6.2） |
> | **D** | 非観血血圧のみ | 間欠的 MAP | 除外。時間分解能が不足 |
>
> **階層 A を主解析集団**とする。心臓外科は同時に**心外膜ペーシングが使える**という決定的な利点を持つ
> （§6.3）。

### 6.2 介入そのものを参照基準に据える

観察研究で SVR の自然変動と ΔRI を突き合わせるより、**既知の薬理学的介入を与えるほうが因果の解釈が
はるかに強い**。

> **決定 6.2** ―― **フェニレフリンのボーラス投与前後**を主要な検証単位とする。理由は、ほぼ純粋な α₁ 刺激で
> あり、**後負荷を選択的に上げる**——本仮説が予測する Γ↑ を最も素直に生む介入だからである。Coutrot 2019 が
> 昇圧薬ボーラスへの血管反応を Dicpleth と PI で追跡した設計の直接の追試にもなる。
>
> 副次的な介入として、**ニカルジピン／ニトログリセリン**（反対向き）、**脊髄くも膜下麻酔の交感神経遮断**
> （明確な SVR 低下）を用いる。

### 6.3 HR 交絡 ― 設計で潰さないと結論が汚染される

**これが本研究の最大の落とし穴である。**フェニレフリンは後負荷を上げると同時に**反射性徐脈**を起こす。
そして RI は HR に依存する。したがって「フェニレフリンで ΔRI が動いた」を観察しても、それが後負荷由来か
HR 由来かが原理的に分離できない。

実験3 は血管パラメータの真値を**一切変えずに** HR だけを 50→110 bpm で動かした結果である。

| HR | landmark RI | 検出率 | 分解 Γ₂ | τ₂ |
|---|---|---|---|---|
| 50 | 0.4039 | 100% | 0.4000 | 420.0 ms |
| 60 | 0.4076 | 100% | 0.3999 | 420.0 ms |
| 70 | 0.4027 | 100% | 0.4008 | 420.1 ms |
| 80 | 0.3621 | 100% | 0.3990 | 419.9 ms |
| 90 | — | **0%** | 0.4013 | 420.1 ms |
| 100 | — | **0%** | 0.4030 | 420.0 ms |
| 110 | — | **0%** | 0.3976 | 420.2 ms |

- **landmark RI は HR 50→80 で 10.3% 低下する。**血管は何も変わっていないので、これは全て見かけの変化である。
- **90 bpm 以上で landmark は完全に検出できなくなる。**拡張期が短縮し、拡張期ピークが次拍の立ち上がりに
  呑まれるためである。術中の頻脈域は珍しくないので、これは実務上の致命傷になりうる。
- **分解 Γ₂ は 0.6% しか動かず、τ₂ は 420 ms でほぼ完全に安定している。**分解法は HR 交絡に対して構造的に
  強い（振幅比を「拡張期に見えた高さ」ではなく「モデル成分の振幅」として定義するため）。

これは本リポジトリの既存知見（「RI は HR と関連するが SI は関連が乏しい」`perioperative_stiffness_outcomes.md`
§6.2）と整合し、その機序を計測側から説明するものでもある。

> **決定 6.3** ―― HR 交絡を三重に潰す。
>
> 1. **設計で。**階層 A（心臓外科）では**心外膜ペーシングで HR を固定**したうえで介入をかける下位研究を置く。
>    HR が定数なら交絡は消える。**これが最も強い設計であり、本研究の中核に据える価値がある。**
> 2. **解析で。**HR を共変量に入れる。AIx@75 に倣った正規化は**そのまま流用しない**——75 bpm 正規化は
>    圧由来 AIx で導かれた経験式であり、PPG 由来 Γ₂ に妥当する保証がない。**本データから HR–指標関係を
>    実測して補正式を作り、それ自体を報告する**。
> 3. **指標選択で。**主解析は分解由来 Γ₂ とする（実験3 のとおり HR 感受性が landmark RI より一桁小さい）。
>
> あわせて、**HR を動かさない昇圧薬との対比**（例：ノルアドレナリン低用量）を副次解析に置くと、
> 後負荷効果と HR 効果の分離がさらに補強される。

### 6.4 評価統計

> **決定 6.4** ―― 主要評価は**変化の一致率**とする。
>
> - **concordance rate**：介入前後で参照（ΔSVR）と指標（ΔΓ₂）が同方向に動いた割合。臨床的に意味のない
>   微小変化を除くため**exclusion zone**を事前設定する
> - **polar plot**：変化の方向と大きさの一致を角度と半径で見る
> - 相関係数（Pearson/Spearman）は**副次**にとどめる。プールした相関は個人内と個人間の情報が混ざる
> - **Bland–Altman は用いない**——絶対値の一致を主張しないので、そもそも適用対象ではない

---

## 7. 検証の段階

`PPG_reflection_wave_localisation.md` §8.5 の段階設計を、本稿の決定に合わせて更新する。

| 段階 | 内容 | 使うデータ | これが失敗したら |
|---|---|---|---|
| **0** | VitalDB で年齢群別に T3〜T0 の出現頻度と、類型別の識別性診断を算出 | VitalDB（公開） | 対象集団の規模が見積もれない。**最優先** |
| **0.5** | **モニタのフィルタ特性の同定**（§4.1） | 実機・既知入力 | モニタ波形での展開が不可能。原波形限定になる |
| **1** | T3・T2 で分解を landmark と突き合わせる（§2.5 の錨） | VitalDB／自施設 | **以降の外挿は成立しない。ここで止める** |
| **2** | ΔΓ₂ と ΔSVR の対応（階層 A） | 心臓外科・熱希釈 | 主仮説の否定 |
| **3** | フェニレフリンボーラス前後の Δ、**ペーシングによる HR 固定下**での再現 | 階層 A 下位研究 | 交絡の分離ができない |
| **4** | 前向き研究（棄却率・評価項目を事前登録） | 自施設 | ― |

> 段階 1 は**ゲート**である。ここを通らずに段階 2 以降へ進んではならない。

---

## 8. 事前に決めて固定すること（事前登録項目）

恣意的な選択が結果に効く箇所を列挙する。**すべて解析開始前に確定させ、変更したら変更履歴を残す。**

1. 基底関数・成分数・β の扱い（§2.4）
2. パラメータの拘束範囲（τ の窓、Γ の上限、前進波幅の上限）
3. landmark 検出の prominence 閾値（**検出率が 100%↔0% で反転しうる**：§2.5）
4. 拍の棄却基準（SQI 閾値、残差閾値、識別性診断の閾値）と、棄却率の報告単位
5. アンサンブル平均の窓長と、急変時の切り替え規則（§4.3）
6. ベースライン B0・B1 の取得条件と再ベースライン事象（§5）
7. 主要評価項目（concordance rate）と exclusion zone の閾値（§6.4）
8. 参照基準の階層と、階層ごとの主張範囲（§6.1）
9. HR 補正式の導出方法（本データから作るのか、既存式を使うのか）（§6.3）
10. 除外基準（`PPG_reflection_wave_localisation.md` §5 の一覧をそのまま原案として使える：大動脈弁閉鎖不全、
    IABP、体外循環、不整脈、測定側の神経ブロック等）

---

## 9. 限界

- **本稿の計測系検証は合成波である。**実 PPG では、ここでモデル化していない要素（呼吸性変動、静脈拍動、
  血管運動、プローブの機械的結合）が加わる。実験1 の「filter-aware で回復率 1.00」は、フィルタが線形時不変で
  特性が既知という理想条件での結果であり、**AGC が非線形・時変であれば同じ論法は使えない**。AGC の実装は
  メーカー非公開のままである（区分 D）。
- **実験1・3 は T3 型の単一波形で行った。**退化波形での帯域制限・HR 交絡の影響は別途評価が要る。
- **反射という枠組み自体がモデル仮定である。**reservoir（windkessel）成分・再反射・上肢局所の寄与が反射項に
  吸収されている可能性は排除できない。成分の解剖学的命名を避けるのはこのためである。
- **PPG は圧ではなく容積信号である。**圧-径関係の非線形により、振幅比は圧由来の指標と互換にならない。
  Γ₂ を「反射係数 Γ」と呼ぶのは近似であり、厳密には容積側で観測された反射の大きさである。
- **本稿は Ea（実効動脈エラスタンス）を主要参照にしていない。**Ea は後負荷の集約指標として理論的には
  望ましいが、SV の連続測定を要し、階層 A に限られる。階層 A では副次評価項目として算出する。

---

## 付録：計測系検証の再現

```
python3 delta_RI_measurement_robustness.py
```

`numpy`・`scipy` に依存する。乱数種は 20260814 で固定。含まれる実験は次の四つ。

| 実験 | 問い | 主な結果 |
|---|---|---|
| 1 | 帯域制限は ΔRI を保存するか | 素朴な分解は 1.0 Hz で Δ の 31% を失う。**filter-aware なら全遮断周波数で回復率 1.00** |
| 2 | AGC は ΔRI を保存するか | ゲイン 8 倍幅で RI・Γ₂・τ₂ は完全に不変、AC（PI 相当）は正比例で変化 |
| 3 | HR は RI を動かすか | 血管条件固定で landmark RI が HR 50→80 で **−10.3%**、90 bpm 以上で**検出率 0%**。分解 Γ₂ は −0.6% |
| 4 | 退化波形とモデル不一致で何が起きるか | T1・T0 で τ₂ 誤差 **約 200 ms**、Γ₂ 相対誤差 43–83%。**初期値ばらつきは 0.0 ms のまま**（＝検出できない） |

---

## 参考文献

本文で新たに参照したもの。既存文書に収録済みの文献は各文書の一覧を参照。

- Millasseau SC, et al. *Clin Sci (Lond)* 2002. SI・RI の定義。PMID 12241535
- Chowienczyk PJ, et al. *J Am Coll Cardiol* 1999. RI と小血管トーヌス。PMID 10588217
- Couceiro R, et al. *Physiol Meas* 2015;36:1801-25. 多ガウス当てはめによる心血管機能評価。**振幅比は
  すべての参照値と低相関**。PMID 26235798
- Basso G, et al. *Physiol Meas* 2024;45(11). skewed-Gaussian モデル。MIMIC-III 8,000 拍で高精度かつ
  初期値に頑健。PMID 39577084
- Tigges T, et al. *EMBC* 2017:4014-7. PDA のモデル選択。最良モデルが全拍の 28.1% でしか選ばれない。
  PMID 29060777
- Goda MÁ, Charlton PH, Behar JA. *Physiol Meas* 2024;45:045001. pyPPG。PMID 38478997
- Pal R, et al. *Comput Methods Programs Biomed* 2024;254:108283. IEM 法による切痕検出。PMID 38901273
- Liu H, et al. *Physiol Meas* 2021;42(7). フィルタ由来の時相シフト。PMID 34111855
- Alian AA, Shelley KH. *Best Pract Res Clin Anaesthesiol* 2014;28:395-406. モニタ表示波形は生信号ではない。
  PMID 25480769
- Pilt K, Reiu A. *Med Biol Eng Comput* 2024;62:1049-59. 経壁圧が指標値を変える。PMID 38123887
- Lee HC, et al. *Sci Data* 2022;9:279. VitalDB。500 Hz の plethysmogram。PMID 35676300
- Coutrot M, et al. *Br J Anaesth* 2019;122:605-12. Dicpleth と PI による低血圧検出・昇圧薬反応追跡。
  doi:10.1016/j.bja.2019.01.037
- Shintani R, Sekino M, Sato S, et al. *Cureus* 2025;17(4):e82131. PI の指間差。示指–薬指が最小
  （0.07 ± 1.89%、p=0.74）。doi:10.7759/cureus.82131
- Middleton PM, et al. 2011. 指尖 PPG と SVR の相関（r=0.54）

---

| **実務メモ** ―― この五つの決定のうち、**明日から着手できて最も律速なのは §4.1 の「フィルタ特性の同定」**である。ここが分からないままモニタ波形を解析しても、Δ が 2〜3 割目減りしていることに気づけない。逆に、フィルタさえ同定できれば帯域制限は克服可能な障害であることが実験1 で示された。次に効くのは §6.3 の**ペーシング下位研究**で、これは心臓外科症例でしか組めないが、HR 交絡を設計で消せる唯一の手段であり、この研究の説得力を決める。 |
| --- |

*本稿は麻酔科臨床の視点からの研究設計案であり、含まれる仮説は前向き検証を経ていない。*
