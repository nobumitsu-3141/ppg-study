# この作業場の決まり

## 1. 用語 ― 造語をしない、比喩で説明しない

**規則の本体は `docs/research/terminology.md`。書く前に読むこと。**

1. **造語をしない。**文献にある用語をそのまま使う。訳語が定まっていないものは原語を併記する。
2. **比喩で説明しない。**「錨」「配管」「土俵」「天井」「帯域」のような言い換えは、
   読み手が原論文に戻れなくなる。難しい内容ほど、余計な語を足さずに書く。
3. **医学・生理の内容は、原文の語か、広く受け入れられている訳語で書く。**
   迷ったら原語をそのまま出す。自分で分かりやすくしようとしない。

これは 2026-09-07 に決めたが、文書に書いただけでは守られなかった（2026-09-09 に
「錨」を再び使った）。**機械で確かめる。**

```sh
python3 analysis/scripts/check_terminology.py
```

`docs/research/terminology_baseline.json` に規則制定前の残件を記録してあり、
**そこから増えたときだけ落ちる。**`docs/manuscript/` は残件を認めない（投稿する文章だから）。
古い文書を直したら `--update-baseline` で記録しなおす。
`--list` で残件を全部出す。`lab_log.md` は 2026-09-07 以降の追記だけを見る
（それ以前は当時の記録なので書き換えない）。

禁止語を**引用する**行（「『錨』は使わない」のような文）は、行末に
`<!-- 用語の引用 -->`（python なら `# 用語の引用`）を置くと検査から外れる。
使用ではなく言及であることを機械は区別できないため。**濫用しないこと。**


よく使ってしまう言い換え（全表は `terminology.md`）:

| 使わない | 使う |
|---|---|
| 錨 | 基準点／照合先（何を指すのか具体に書く） |
| 窓 | ウィンドウ（`窓関数`・`Kaiser 窓` は可） |
| 鍵点 | 特徴点（fiducial point） |
| 肩 | 変曲点（下降脚の変曲点） |
| 天井 | 上限／到達しうる最大値 |
| 帯域 | 説明できる分散の大きさ（`帯域制限` は可） |
| 化ける | 別の量になる／〜の解釈に変わる |
| 足切り | 除外基準／不成立と判定する条件 |
| 予行 | 予備的検討／同じ手順を先に試すこと |
| 配管／土俵 | 解析の流れ・処理系／データセット・条件 |

## 2. シェル

**コマンドの後ろに `#` のコメントを付けない。**利用者の環境は zsh で、
`--selftest # 確認` のように書くと `unrecognized arguments: #` で落ちる。
説明はコードブロックの外に書く。

## 3. 解析の進め方

- **事前に決めた閾値・規準は、結果を見たあとで変えない。**変える場合は
  変えたことと理由を `docs/research/lab_log.md` に必ず残す。
- **やったことは `lab_log.md` に追記する。**失敗と、その原因も書く。
- **検証してから commit し、push する。**スクリプトには `--selftest` を用意し、通してから出す。
- **数値の報告は出典を明示する。**どのスクリプト・どの対象・何例・何ウィンドウか。

## 4. 解析スクリプトを別のマシンで動かすとき

- `analysis/data/` は git に入っていない（`.gitignore`）。
- `37_ri_svr_screen.py`・`38_couceiro_svr.py` は `data/features/`・`data/vasotone/` が要る。
  **これらがある 1 台目でしか動かない。**
- `39_ri_svr_standalone.py` は自己完結で、ネットワークだけで最初から走る。
